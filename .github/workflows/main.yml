name: Example Workflow

on: [push]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: 22f3001839@ds.study.iitm.ac.in
        run: echo "Hello, world!"
